# CLAUDE.md

Project rules for this repository live in [AGENTS.md](AGENTS.md). Read it first —
it is the canonical agent brief and carries the document map, the workflow rules
(demo every user-visible feature, never work around a bug, file code smells), and
the build/test/format steps required before every commit.

This file exists only so those rules load automatically. Keep it a pointer; add
new rules to `AGENTS.md` or the docs it links, not here.
